import type { Context, DynamoDBStreamEvent } from 'aws-lambda';
import { handler } from '../../src/handler';

describe('Test confirmation email lambda function', () => {
  test.skip('do something', async () => {
    // TODO
  });
});
